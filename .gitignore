node_modules/
*.log

# Segredos - nunca commitar
.env
.env.*
!.env.example

# Banco de dados local - contém dados reais de clientes/pedidos
data/*.db
data/*.db-shm
data/*.db-wal

# Uploads de usuários - não devem ir pro controle de versão
uploads/*
!uploads/.gitkeep